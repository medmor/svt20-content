# Daily Content History

Archived daily content files. Each file is named by date (YYYY-MM-DD.json).

## Format
Same as `daily-content.json` with an optional `revisions` array tracking corrections:
- `timestamp`: when the correction was made
- `changes`: human-readable description
- `fields`: the corrected field values
